[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow
    smoke: core smoke tests
addopts = -v --tb=short --strict-markers --headed
